// CHECK: Check OK
mod When {
    incoming clock : Clock
    incoming inp1 : Bit on clock

    outgoing out1 : Bit on clock {
        when {
            case inp1 {
                it := true
            }
            else {
                it := false
            }
        }
    }

    reg r : Bit on clock {
        when {
            case inp1 {
                it <= true
            }
        }

        unused it
    }

    incoming inp2 : Bit on clock

    outgoing out2 : Bit on clock {
        it := when {
            case inp2 => false
            else => true
        }
    }
}

mod Fine {
    incoming clock : Clock
    incoming reset : Reset
    outgoing out : Word[3] on clock

    reg start : Word[3] on clock {
        it <= when {
            case reset => 0
            else => it
        }
    }
    out := start
}
