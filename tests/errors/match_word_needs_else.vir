// MatchCoverage: Word[8] subject with only a few literals and no `else`.

mod WordNeedsElse {
    incoming x : Word[8]
    outgoing out : Bit
    // CHECK: Non-exhaustive match
    out := match x {
        case 0 => true
        case 1 => false
        case 2 => true
    }
}

// CHECK: Check failed
