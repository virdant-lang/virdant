// Tests for empty driver blocks: components, submodules, and sockets
// whose it-blocks contain no drivers.
// Each empty driver block should produce a warning.

mod ComponentModule {
    incoming a : Bit async {
        b := it
    }

    // CHECK: Empty driver block
    outgoing b : Bit async {
    }
}

// CHECK-NOT: Check failed
