// Multi-line continuation: driver with RHS on next line
mod Continuation {
    incoming x : Word[8] async
    incoming y : Word[8] async

    outgoing sum : Word[8] async

    sum :=
        x + y
}
