mod Control {
    incoming opcode : Word[7];

    outgoing rf_write : Bit;

    rf_write := if opcode == 19 { // OP-IMM
        true
    } else if opcode == 52 { // OP
        true
    } else {
        false
    };
}
