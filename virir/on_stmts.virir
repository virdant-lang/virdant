virir {
    package on_stmts {
        export mod Top {
            incoming clock : builtin::Clock;

            on clock {
                assert((1 : builtin::Bit));
                display();
                fatal;
            }
        }
    }

    type builtin::Bit;
    type builtin::Clock;
}
