mod Mux2 {
    incoming a : Word[8]
    incoming b : Word[8]
    incoming select : Bit

    outgoing out : Word[8] {
        it := if select {
            a
        } else {
            b
        }
    }
}
