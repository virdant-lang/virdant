union type MaybeWord {
    Nothing()
    Something(payload : Word[8])
}

mod Top {
    incoming inp : MaybeWord
    outgoing out : Word[8]

    out :=
        match inp {
            case @Nothing() => 0
            case @Something(payload) => payload
        }
}
