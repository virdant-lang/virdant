mod Mux2 {
    incoming a : Word[8]
    incoming b : Word[8]
    incoming select : Bit

    outgoing out : Word[8] {
        it := when {
            case select => a
            else => b
        }
    }
}
