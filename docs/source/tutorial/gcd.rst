GCD
===
In this next example, we will demonstrate union types
We will also see the pattern for resetting a register.

`Gcd` is a circuit which calculates the greatest common divisor of two numbers.

.. literalinclude:: /examples/gcd.vir
    :caption: gcd.vir
    :language: virdant


What it Does
------------
asdf
