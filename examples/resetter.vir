mod Resetter {
    incoming clock : Clock
    outgoing out : Bit

    reg countdown : Word[32] on clock
    countdown <= if countdown == 0 {
        countdown
    } else {
        countdown - 1
    }

    out := countdown == 1
}
