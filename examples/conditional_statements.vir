mod Top {
    incoming cond : Bit
    wire w : Bit
    wire u : Bit

    when {
        case cond {
            w := true
            u := true
        }
        else {
            w := false
            u := false
        }
    }
}
