mod Top {
    incoming cond : Bit async
    wire w : Bit async
    wire u : Bit async

    when {
        case cond {
            w := true
            u := true
        }
        else {
            w := false
            u := false
        }
    }
}
