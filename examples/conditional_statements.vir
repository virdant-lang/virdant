mod Top {
    wire w : Bit
    wire u : Bit

    when {
        case true {
            w := true
            u := true
        }
        else {
            w := false
            u := false
        }
    }
}
