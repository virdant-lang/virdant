#!/bin/bash
set -ex

project=${1%/}

mkdir -p "build/$project"

vir compile-to-virir "$project" "build/${project}.virir" 2> /dev/null
vir-ir "build/${project}.virir" --builddir="build/$project"
iverilog verilog/tb.sv "build/$project/"*.sv -o "build/${project}/run"
"./build/${project}/run"
